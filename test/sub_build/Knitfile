foo = dofile("libfoo/Knitfile")

return r{
$ %.o: %.c
    cc -c $input -o $output

$ prog: f1.o f2.o f3.o [$foo][libfoo]libfoo.a
    cc $input -o $output

$ clean:VB: [$foo][libfoo]clean
    rm -f *.o prog
}
