foo = include("libfoo/build.knit")
bar = include("libfoo/libbar/build.knit")

return {
    r{
    $ %.o: %.c
        cc -c $input -o $output
    $ prog: f1.o f2.o f3.o libfoo/libfoo.a
        cc $input -o $output
    $ clean:VB: libfoo/clean
        rm -f *.o prog
    },
    foo, bar
}
