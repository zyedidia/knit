set cc gcc
set cflags "-O2 -Wall"

rules {

%.o: %.c
    $cc $cflags -c $in -o $out
hello: hello.o other.o
    $cc $cflags $in -o $out
clean:V:
    rm -f hello.o other.o hello
echo:VQ:
    echo hello!

}
