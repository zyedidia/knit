knit = import("knit")

cc = cc or "gcc"
cflags = "-Wall"
src = knit.Glob("*.c")
obj = knit.ExtRepl(src, ".c", ".o")

date = knit.Shell("date")

$ all:V: hello.o

$ clean:V:
    rm -f hello.o

$ %.o: %.c
    $cc $cflags -c $in -o $out

$ echo-%:VQ:
    echo ${eval(stem)}
