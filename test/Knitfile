knit = import("knit")
fmt = import("fmt")
filepath = import("path/filepath")

cc = cc or "gcc"
cflags = "-Wall"
src = filepath.Glob("*.c")
obj = knit.ExtRepl(src, ".c", ".o")

function hello()
    return "-Wall"
end

# %.o: %.c
    $cc $cflags -c $src -o $ob
