name = "Check direct rules are chosen over meta-rules"

[flags]

knitfile = "Knitfile"
ncpu = 1
dryrun = true

[[builds]]

args = ["f2.o"]
output = "cc f2.c"

[[builds]]

args = ["f1.o"]
output = "cc -Wall f1.c"
